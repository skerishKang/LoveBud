const fs=require('fs');const h=fs.readFileSync('functions/api/scout/live-rate-limit-storage-hash-helper.js','utf8');for(const s of ['VERSION','sanitizeHashPayload','userKeyHash','hashPreview:null']){if(!h.includes(s))process.exit(1);}for(const s of ['crypto.subtle','createHash','SCOUT_RATE_LIMIT_KV']){if(h.includes(s))process.exit(1);}