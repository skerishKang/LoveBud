const fs=require('fs');const h=fs.readFileSync('functions/api/scout/live-rate-limit-storage-hash-helper.js','utf8');for(const s of ['VERSION','ALLOWED_INPUTS','PROHIBITED_INPUTS','hashPreview:null','STORAGE_HASH_HELPER_DISABLED']){if(!h.includes(s))process.exit(1);}for(const s of ['crypto.subtle','createHash','SCOUT_RATE_LIMIT_KV','SCOUT_RATE_LIMIT_D1']){if(h.includes(s))process.exit(1);}