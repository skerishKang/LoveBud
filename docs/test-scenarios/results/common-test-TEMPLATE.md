# 테스트 결과 - 노드 반복 생성 테스트

> ⚠️ **사용 방법**: 이 파일을 복사하여 `results/{그룹명}-{userType}-test-YYYY-MM-DD-HHMM/test-result.md` 형식으로 저장하세요
>
> **userType 종류**:
> - `newuser`: 신규 가입자 (회원가입부터)
> - `existing`: 기존 가입자 (로그인부터)
>
> **폴더 구조 예시**:
> ```
> results/
> └── ive-newuser-test-2026-04-18-1430/
>     ├── test-result.md          ← 이 파일 (결과 기록)
>     ├── step1-home.png          ← 스크린샷
>     ├── step2-login.png
>     └── ...
> ```

---

## 테스트 메타데이터

- **시나리오 ID**: REPEAT-NODE-001
- **테스트 대상 그룹**: {아이돌 그룹명}
- **테스트 일시**: 2026-XX-XX XX:XX
- **테스트 수행자**: (이름/ID)
- **테스트 환경**: (브라우저/OS)
- **사용 데이터**: `data/{그룹명}-data.json`

### 사용자 유형 (필수 선택)

- **userType**: `newuser` / `existing`
  - `newuser`: 신규 가입자 (회원가입부터 테스트)
  - `existing`: 기존 가입자 (로그인부터 테스트)

### 계정 정보

| 항목 | 신규 가입자 (newuser) | 기존 가입자 (existing) |
|------|----------------------|----------------------|
| **계정 생성 방식** | 테스트 중 신규 생성 | `.local/test-accounts.json` 참조 |
| **이메일** | `test-{timestamp}@test.com` | (저장소에 기록 금지) |
| **비밀번호** | `TestPass123!` | (저장소에 기록 금지) |
| **계정 출처** | data/new-user-flow.json | .local/test-accounts.json |

---

## STEP 2: 인증 흐름

### 사용자 유형에 따른 분기

| 사용자 유형 | 진입 방식 | 기록 포인트 |
|------------|----------|------------|
| **신규 가입자** | 회원가입부터 시작 | 회원가입 소요 시간, 헷갈린 부분 |
| **기존 가입자** | 로그인부터 시작 | 로그인 소요 시간, 자동 로그인 여부 |

### 기록 형식

**신규 가입자 (newuser)**:
```markdown
**STEP 2: 회원가입**
- 가입 버튼 발견 시간: (초)
- 이메일 입력: (패턴)
- 비밀번호 입력: (규칙 충족 여부)
- 가입 완료 소요 시간: (초)
- 막힌 지점:
```

**기존 가입자 (existing)**:
```markdown
**STEP 2: 로그인**
- 로그인 버튼 발견 시간: (초)
- 이메일 입력 방식: (자동완성/수동)
- 로그인 완료 소요 시간: (초)
- 자동 로그인 여부: (예/아니오)
- 막힌 지점:
```

---

## STEP 1: 서비스 접속

### 결과

**이해도**: (예/아니오 + 이유)

**첫 인상**: 

**혼란 포인트**:

---

## STEP 3: 트리 생성

### 결과

| 항목 | 값 |
|------|-----|
| 트리 생성까지 클릭 수 | (회) |
| 멈춘 지점 | |
| 직관성 평가 | (1-10) |

---

## STEP 4: 노드 생성 (핵심) ⭐

### 노드 1

- **URL**: (data 파일에서)
- **성공 여부**: (예/아니오)
- **소요 시간**: (초)
- **어려움/메모**:

### 노드 2

- **URL**:
- **성공 여부**:
- **소요 시간**:
- **어려움/메모**:

### 노드 3

- **URL**:
- **성공 여부**:
- **소요 시간**:
- **어려움/메모**:

### 노드 4

- **URL**:
- **성공 여부**:
- **소요 시간**:
- **어려움/메모**:

---

## STEP 5: 노드 5

### 노드 5

- **URL**: (data 파일에서)
- **성공 여부**: (예/아니오)
- **소요 시간**: (초)
- **어려움/메모**:

---

## STEP 6: 반복 테스트 ⭐⭐

### 결과 테이블

| 노드 # | 소요 시간 | 귀찮음 정도 (1-10) | 특이사항 |
|--------|-----------|-------------------|----------|
| 5 | | | |
| 6 | | | |

### 반복 임계점 분석

- **몇 번째부터 귀찮았는가?**: 
- **어떤 부분이 반복해서 불편했는가?**:

---

## STEP 7: 트리 상태 확인

### 결과

| 항목 | 값 |
|------|-----|
| 총 노드 수 | (6개) |
| 구조 가독성 | (1-10) |
| 직관성 평가 | |

---

## 강제 테스트 (에지 케이스)

| 테스트 | 결과 | 메모 |
|--------|------|------|
| 잘못된 URL 입력 | (성공/실패/예상외) | |
| 뒤로가기 | (상태유지/초기화/오류) | |
| 새로고침 | (데이터유실/유지) | |
| 로그인 없이 접근 | (리다이렉트/가드작동) | |

---

## 최종 평가

### 메트릭

| 항목 | 값 |
|------|-----|
| 전체 완료 가능 여부 | (예/아니오) |
| 총 소요 시간 | (분:초) |
| 노드 생성 난이도 | (1~10, 1=매우쉬움) |
| 반복 사용 가능성 | (1~10, 1=전혀못함) |

### 핵심 질문

> **"이 작업을 계속 반복해서 사용할 수 있는가?"**

- **답변**: YES / NO
- **이유**: 

---

## UX 개선 포인트

### 발견된 문제점 (우선순위 높음)

1. 
2. 
3. 

### 개선 제안

1. 
2. 
3. 

---

## 참고 자료

- **스크린샷 폴더**: `screenshots/{그룹명}-test-YYYY-MM-DD-HHMM/`
- **비디오 녹화**: (있는 경우 경로)
- **콘솔 로그**: (있는 경우)

---

## 다음 테스트 필요사항

- [ ] 재테스트 필요
- [ ] 특정 문제 해결 후 재검증
- [ ] 통과 - 다음 그룹/시나리오 진행

---

*생성된 파일명: `results/{그룹명}-{userType}-test-YYYY-MM-DD-HHMM/test-result.md`*
*예시: `ive-newuser-test-2026-04-18-1430/test-result.md`, `bts-existing-test-2026-04-18-1500/test-result.md`*

*스크린샷 저장 위치: 동일 폴더 내 `step1-home.png`, `step2-login.png` 등*
