# Archive README

This directory contains archived operational documents and workflows that are no longer active or have been superseded.

## Archived items

- `deploy-test-slot.workflow.yml.txt` — GitHub Actions workflow for fixed-slot deployment (deprecated; superseded by local Wrangler OAuth deploy)

## Archive policy

- Archives are kept for historical reference and audit trail.
- Active operational documents reside in parent `docs/ops/` directory.
- For current fixed-slot deployment procedure, see:
  `docs/ops/FIXED_SLOT_DEPLOY_WITH_WRANGLER.md`
