# LoveBud 환경변수 예시 파일
# 복사해서 .env 파일로 사용하세요 (gitignore에 추가됨)
#
# 상세 문서: docs/ops/ENV_DEPENDENCY.md
# 배포 체크리스트: docs/ops/DEPLOY_CHECKLIST.md

# ─────────────────────────────────────────────────────────────────────────────
# Neon PostgreSQL (필수)
# ─────────────────────────────────────────────────────────────────────────────
# 누락 시: 모든 API 503, "Database is not configured"
# Netlify에서 사용하는 변수명 (권장)
NETLIFY_DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-XXXX.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# 또는 일반적인 DATABASE_URL도 지원
# DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-XXXX.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# ─────────────────────────────────────────────────────────────────────────────
# Firebase (Netlify Functions용, 인증 필요 API에 필수)
# ─────────────────────────────────────────────────────────────────────────────
# 누락 시: POST /api/trees, POST/GET /api/memories 등 인증 필요 API 503
# GET /api/trees (공개 트리), GET /api/community/memories는 영향 없음
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# ─────────────────────────────────────────────────────────────────────────────
# 시드 스크립트 옵션
# ─────────────────────────────────────────────────────────────────────────────
# DRY_RUN=true  # 실제 삽입 없이 미리보기만
# SEED_STAGE=phase1  # phase1 | phase2 | all

# ─────────────────────────────────────────────────────────────────────────────
# 검증 스크립트
# ─────────────────────────────────────────────────────────────────────────────
# node scripts/verify-env.js                          # 로컬 환경변수 + 함수 문법 확인
# node scripts/verify-env.js --remote https://lovebud.netlify.app  # 원격 엔드포인트 확인
